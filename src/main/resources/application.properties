

# MySQL Configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.datasource.url=jdbc:mysql://${DATABASE_HOST}/${DATABASE_NAME}?serverTimezone=UTC
#spring.datasource.username=${DATABASE_USERNAME}
#spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.url=${DB_URL}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.username=${DB_USERNAME}


spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true


#upload.path=C:/workspace/musicAlbum/src/main/resources/static/upload
upload.path=/Users/dean/IdeaProjects/musicAlbum/src/main/resources/static/upload
# logging
logging.level.org.springframework=INFO
logging.level.org.hibernate=ERROR

# Storage Setting
aws.s3.endpoint=${STORAGE_ENDPOINT}
aws.s3.region=auto
aws.s3.access-key=${STORAGE_ACCESS_KEY}
aws.s3.secret-key=${STORAGE_SECRET_KEY}